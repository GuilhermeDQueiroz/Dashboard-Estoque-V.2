/*const express = require('express');
const router = express.Router();
const controller = require('../controllers/docfatController');

router.get('/docfat', controller.listarDocfat);
router.get('/docfat/:id', controller.buscarPorId);

module.exports = router;*/
